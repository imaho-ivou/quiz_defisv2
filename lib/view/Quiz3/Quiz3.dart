import 'package:flutter/material.dart';

class QUiz3 extends StatelessWidget {
  const QUiz3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quiz3'),
      ),
      body: Center(
        child: Container(),
      ),
    );
  }
}
